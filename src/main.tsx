import React from 'react';
import ReactDOM from 'react-dom/client';
import LotoPredictor from './LotoPredictor';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <LotoPredictor />
  </React.StrictMode>
);
